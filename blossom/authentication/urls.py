from django.contrib import admin
from django.urls import path

from blossom.website import views

urlpatterns = [

]

# This file is not used anywhere; it's only used by the hosts file to move
# the endpoint from example.com/admin to admin.example.com.
# This effectively treats the admin site as a standalone subdomain of the
# website.
